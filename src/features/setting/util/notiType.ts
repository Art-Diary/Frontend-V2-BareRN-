export type NotiInfoType = {
  notiId: number;
  notiCode: string;
  notiName: string;
  notiSubText: string;
  notiState: boolean;
};

export type NotiRequestUType = {
  notiId: number;
  setNoti: boolean;
};
